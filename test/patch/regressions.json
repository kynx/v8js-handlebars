{
    "regressions-bug reported by @fat where lambdas weren't being properly resolved-00": {
        "data": {
            "thing": {
                "phpstub": "function () {\n        return 'blah';\n      }"
            },
            "hasThings": {
                "phpstub": "function () {\n        return true;\n      }"
            }
        }
    },
    "regressions-gh-1054: should handle simple safe string responses-00": {
        "helpers": {
            "wrap": {
                "phpstub": "function (options) {\n        return new Handlebars.SafeString(options.fn());\n      }"
            }
        }
    },
    "regressions-gh-1093: undefined helper context-00": {
        "helpers": {
            "helper": {
                "phpstub": "function () {\n        \/\/ It's valid to execute a block against an undefined context, but\n        \/\/ helpers can not do so, so we expect to have an empty object here;\n        for (var name in this) {\n          if (this.hasOwnProperty(name)) {\n            return 'found';\n          }\n        }\n        \/\/ And to make IE happy, check for the known string as length is not enumerated.\n        return (this == 'bat' ? 'found' : 'not');\n      }"
            }
        }
    },
    "regressions-gh-837: undefined values for helpers-00": {
        "helpers": {
            "str": {
                "phpstub": "function (value) { return value + ''; }"
            }
        }
    },
    "regressions-gs-428: nested if else rendering-00": {
        "helpers": {
            "blk": {
                "phpstub": "function (block) { return block.fn(''); }"
            },
            "inverse": {
                "phpstub": "function (block) { return block.inverse(''); }"
            }
        }
    },
    "regressions-gs-428: nested if else rendering-01": {
        "helpers": {
            "blk": {
                "phpstub": "function (block) { return block.fn(''); }"
            },
            "inverse": {
                "phpstub": "function (block) { return block.inverse(''); }"
            }
        }
    }
}