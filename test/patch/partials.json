{
    "inline partials-should override global partials-00": {
        "partials": {
            "myPartial": {
                "phpstub": "function () { return 'fail'; }"
            }
        }
    },
    "partials-dynamic partials-00": {
        "helpers": {
            "partial": {
                "phpstub": "function () {\n        return 'dude';\n      }"
            }
        }
    },
    "partials-dynamic partials-01": {
        "helpers": {
            "partial": {
                "phpstub": "function () {\n        return 'dude';\n      }"
            }
        }
    },
    "partials-failing dynamic partials-00": {
        "helpers": {
            "partial": {
                "phpstub": "function () {\n        return 'missing';\n      }"
            }
        }
    },
    "partials-rendering function partial in vm mode-00": {
        "partials": {
            "dude": {
                "phpstub": "function partial(context) {\n      return context.name + ' (' + context.url + ') ';\n    }"
            }
        }
    },
    "partials-global partials-01": null,
    "partials-throw on missing partial-00": {
        "template": "{{> nonexistent}}",
        "message": "/The partial nonexistent could not be compiled/",
        "note": "spec actually unsets dude partial then tries to use it"
    },
    "partials-throw on missing partial-01": null,
    "should pass compiler flags-should pass compiler flags-00": {
        "template": "{{foo}} {{> partial}}",
        "globalPartials" : {
            "partial" : "{{foo}}"
        },
        "data" : {
            "foo": "<"
        },
        "compileOptions": {
            "noEscape": true
        },
        "note": "no idea why these aren't picked up"
    }
}
